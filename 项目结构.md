# 项目结构

## 目录总览

```
Teaching-Assistant-main/
├── app/                          # 应用层
│   ├── __init__.py               # 包标记
│   ├── audio_listener.py         # 实时音频监听（系统声音/麦克风/混合）
│   ├── config.py                 # 集中配置管理（环境变量 + 默认值）
│   ├── doubao_streaming_asr.py   # 豆包大模型流式 ASR（WebSocket 二进制协议）
│   ├── llm.py                    # 大模型回答生成（豆包/OpenAI/DeepSeek/通义千问/本地兜底）
│   ├── main.py                   # 命令行入口
│   ├── question_handler.py       # 学生提问处理（检索 + 上下文构建 + 回答生成）
│   ├── transcriber.py            # 音频转写调度（豆包音频理解/OpenAI/Vosk）
│   ├── web_main.py               # Web UI 启动入口
│   └── web_server.py             # Flask 后端（REST API + SSE 实时推送）
├── rag/                          # RAG 检索增强
│   ├── __init__.py               # 包标记
│   ├── chunking.py               # 文本切分
│   ├── pipeline.py               # 入库 + 检索 + 上下文构建主流程
│   ├── transcript_store.py       # JSONL 课堂记录存储
│   └── vector_index.py           # 本地向量索引（TF-IDF + 余弦相似度）
├── static/                       # Web 前端
│   └── index.html                # 三栏暗色 SPA（左控制/中问答/右实时转写）
├── data/                         # 运行时数据（.gitignore 已忽略）
├── models/                       # 本地语音模型（Vosk）
├── sample/                       # 早期示例代码（参考用，非运行依赖）
├── scripts/                      # 辅助脚本
│   └── save_ark_key.ps1          # 写入 .env 的火山方舟 Key
├── tests/                        # 自动化测试
│   └── test_pipeline.py
├── ppt/                          # 分享用演示 PPT（瑞士国际主义风格）
├── unused/                       # 已废弃代码
├── .env                          # 运行时环境变量（不提交）
├── .env.example                  # 环境变量模板
├── .gitignore
├── requirements.txt              # Python 依赖
├── README.md                     # 项目总说明
├── 项目结构.md                    # 本文件
└── 需求文档.md                    # 需求与工作流说明
```

## 文件说明

### app/ — 应用层

| 文件 | 职责 |
|------|------|
| `config.py` | `Settings` 数据类，所有配置项集中管理，从环境变量 / `.env` 读取 |
| `audio_listener.py` | 实时音频采集 + 转写调度。支持三种音源（系统/麦克风/混合），内含短句合并缓冲区（≥80字或5秒强制入库），partial text 去重（1.5秒节流 + 内容比对） |
| `doubao_streaming_asr.py` | 豆包大模型流式 ASR 的 WebSocket 客户端，负责二进制协议打包/解析、实时 PCM 发送、partial/final 句子回调 |
| `transcriber.py` | 音频转写调度器，根据配置选择豆包音频理解 / OpenAI / 本地 Vosk，管理流式片段 vs 固定片段模式 |
| `llm.py` | 回答生成，支持豆包 / OpenAI / DeepSeek / 通义千问四个 LLM 提供方，以及本地关键词兜底 |
| `question_handler.py` | 学生提问入口，串联 RAG 检索 → 上下文构建 → LLM 生成 |
| `main.py` | 命令行交互入口，提供 `/start` `/stop` `/note` `/ask` `/clear` `/source` 等命令 |
| `web_server.py` | Flask 应用工厂，提供 REST API + SSE 实时推送，含 `SSEEventBus` 事件总线 |
| `web_main.py` | Web UI 启动脚本，在 `127.0.0.1:5001` 起 Flask 服务 |

### rag/ — RAG 检索增强

| 文件 | 职责 |
|------|------|
| `chunking.py` | 将长文本按 `chunk_max_chars`（默认800字）切分，`chunk_overlap`（默认120字）重叠 |
| `pipeline.py` | RAG 主流程：老师内容入库 → 向量化 → 索引更新；学生提问 → 检索 → 上下文构建 |
| `transcript_store.py` | JSONL 格式课堂记录，支持追加、查看最近、清空 |
| `vector_index.py` | 纯标准库实现的 TF-IDF 向量索引 + 余弦相似度检索，无需外部向量库 |

### static/ — Web 前端

| 文件 | 职责 |
|------|------|
| `index.html` | 三栏暗色主题 SPA。左栏：听课控制（开始/停止/手动记/清空/音源切换含混合模式）；中栏：问答对话（含清空对话按钮）；右栏：SSE 驱动的实时课堂转写 |

### 其他目录

| 目录 | 说明 |
|------|------|
| `data/` | 运行时生成 `transcripts.jsonl`（课堂记录）和 `vector_index.json`（向量索引），已加入 `.gitignore` |
| `models/` | 本地 Vosk 中文小模型，`auto` 模式无云端 Key 时回退使用 |
| `sample/` | 早期原型代码和示例，仅供参考 |
| `scripts/` | `save_ark_key.ps1`：一键写入火山方舟 API Key 到 `.env` |
| `tests/` | `test_pipeline.py`：文本切分、入库、检索、回答、清空等测试 |
| `ppt/` | 瑞士国际主义风格演示 PPT（单 HTML 文件），用于读书分享会 |
| `unused/` | 已废弃代码，保留备查 |

## 运行时生成文件

| 文件 | 位置 | 说明 |
|------|------|------|
| `transcripts.jsonl` | `data/` | 课堂记录，每行一条 JSON（source + text + created_at） |
| `vector_index.json` | `data/` | TF-IDF 向量索引，增量更新 |

两个文件均在 `.gitignore` 中，不提交到仓库。

## 更新规则

- 新增/删除/移动文件后，同步更新本目录结构和文件说明
- 运行时文件不需要逐条记录，只需记录其所在目录用途
- 当某文件职责发生变化时，更新对应说明
