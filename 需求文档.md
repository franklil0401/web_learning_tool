# 需求文档 — 学生听课实时问答助手

## 1. 项目目的

学生在直播课、录播课或线下课堂听讲时，往往来不及消化老师的每一句话。本项目做一个**课堂旁听助手**：一边持续监听并转写老师讲课内容，一边把转写文本整理成可检索的知识库；学生随时提问时，系统从已记录的课程内容中检索相关片段，调用大模型生成简洁准确的回答。

核心定位：**不是替代老师，而是学生的第二双耳朵**——帮你记住、帮你找到、帮你理解。

## 2. 核心需求

### 2.1 课堂内容实时采集

系统持续监听课堂声音来源，将老师讲话实时转写为文本。

| 需求项 | 说明 |
|--------|------|
| 中文语音识别 | 支持中文课程，首选豆包大模型流式 ASR（WebSocket 真流式），回退到本地 Vosk |
| 持续监听 | 开始后持续录音，直到手动停止 |
| 三种音源 | ① 电脑系统声音（适合 Bilibili/网课平台）；② 麦克风（适合线下课堂）；③ 混合模式（同时录系统+麦克风，适合腾讯会议戴耳机场景） |
| 短句合并 | ASR 输出的碎片句子（如"呃，今天是这个。"仅7字）先积累到 ≥80 字再入库，超过 5 秒无新句也强制入库，避免 RAG 切片过于细碎 |
| Partial 去重 | 流式 ASR 会反复推送同一段 partial 文本，需在 ASR 客户端层和 AudioListener 层双重去重 |

### 2.2 课程知识实时入库

老师讲课内容不能只停留在转写结果中，必须进入可检索的知识库。

| 需求项 | 说明 |
|--------|------|
| 实时入库 | 每段最终识别文本立即写入课堂记录 + 更新向量索引 |
| 文本切分 | 长文本按 800 字切分，120 字重叠，保证上下文连贯 |
| 向量索引 | 使用 TF-IDF + 余弦相似度，纯标准库实现，无需外部向量库 |
| 增量更新 | 课程进行中可以边听边问，索引持续更新 |

### 2.3 学生实时提问

学生随时输入自然语言问题，系统从已记录的课程内容中检索并回答。

| 需求项 | 说明 |
|--------|------|
| 语义检索 | Top-5 召回，最低相似度阈值 0.05，兼顾召回率与准确率 |
| LLM 回答 | 支持豆包 / OpenAI / DeepSeek / 通义千问，回答基于课堂内容 |
| 容错提示 | LLM 提示词明确要求：不要因为碎片化就拒绝回答，从碎片中推断和综合 |
| 参考片段 | 回答下方展示检索到的相关课堂片段及相似度分数，增强可信度 |
| 内容不足 | 课堂内容完全无关时，明确提示"当前课堂内容不足" |

### 2.4 会话管理

| 需求项 | 说明 |
|--------|------|
| 开始/停止 | 随时开始或停止听课 |
| 清空历史 | 一节课结束后一键清空，避免课程间内容串扰 |
| 清空对话 | Web UI 中可单独清空问答对话区（不影响课堂记录） |

## 3. 环境与依赖

### 3.1 运行环境

- **Python** 3.11+
- **操作系统**：Windows（音频采集依赖 Windows API）
- **浏览器**：Chrome / Edge / Firefox（Web UI 使用 SSE，需支持 EventSource）

### 3.2 Python 依赖

核心功能只依赖 Python 标准库。以下按使用场景按需安装：

| 场景 | 依赖 | 安装命令 |
|------|------|----------|
| 电脑系统声音监听 | soundcard, numpy | `pip install soundcard numpy` |
| 麦克风输入 | sounddevice | `pip install sounddevice` |
| 本地语音识别（回退） | vosk | `pip install vosk` |
| 豆包流式 ASR | websockets | `pip install websockets` |
| Web UI | flask | `pip install flask` |
| .env 文件加载 | python-dotenv | `pip install python-dotenv` |
| 豆包 LLM 回答 | openai（火山方舟兼容） | `pip install openai` |
| 通义千问回答 | dashscope | `pip install dashscope` |

一键安装全部可选依赖：

```bash
pip install soundcard numpy sounddevice vosk websockets flask python-dotenv openai dashscope
```

### 3.3 API 密钥配置

项目需要两类 API 凭证，均在火山引擎控制台申请：

**① 火山方舟 API Key（用于 LLM 回答）**
- 控制台：https://console.volcengine.com/ark
- 环境变量：`ARK_API_KEY`

**② 豆包大模型流式语音识别凭证（用于 ASR 转写）**
- 控制台：https://console.volcengine.com/speech/service
- 环境变量：`TA_DOUBAO_ASR_APP_KEY`、`TA_DOUBAO_ASR_ACCESS_KEY`
- 两者与 `ARK_API_KEY` 不是同一个东西

### 3.4 .env 配置

将 `.env.example` 复制为 `.env`，填入真实 Key。推荐配置（豆包 ASR + 豆包 LLM）：

```env
# ── LLM 回答 ──
ARK_API_KEY=你的火山方舟APIKey
TA_LLM_PROVIDER=doubao
TA_DOUBAO_ANSWER_MODEL=doubao-seed-2-0-lite-260428

# ── 流式语音识别 ──
TA_TRANSCRIPTION_PROVIDER=doubao_streaming
TA_DOUBAO_ASR_APP_KEY=你的ASR_App_Key
TA_DOUBAO_ASR_ACCESS_KEY=你的ASR_Access_Key
TA_DOUBAO_ASR_RESOURCE_ID=volc.bigasr.sauc.duration
```

### 3.5 本地 Vosk 模型

当未配置云端 API 时，系统回退到本地 Vosk 识别。默认路径：

```
models/vosk-model-small-cn-0.22/
```

如放在其他位置，设置环境变量 `TA_SPEECH_MODEL_PATH` 或写入 `.env`。

## 4. 工作流程

### 4.1 整体数据流

```
┌─────────────┐     ┌───────────────┐     ┌──────────────┐     ┌──────────────┐
│  音频采集    │────▶│  语音转写      │────▶│  短句合并     │────▶│  RAG 入库     │
│ 系统声/麦克风 │     │ 豆包ASR/Vosk  │     │ ≥80字/5秒    │     │ 切分+向量化   │
└─────────────┘     └───────────────┘     └──────────────┘     └──────────────┘
                                                                        │
                                                                        ▼
┌─────────────┐     ┌───────────────┐     ┌──────────────┐     ┌──────────────┐
│  回答输出    │◀────│  LLM 生成     │◀────│  RAG 检索     │◀────│  学生提问     │
│ 终端/Web UI  │     │ 豆包/DS/OpenAI│     │ Top5+相似度   │     │ 自然语言      │
└─────────────┘     └───────────────┘     └──────────────┘     └──────────────┘
```

### 4.2 启动流程

有两种运行方式：

**方式一：命令行（CLI）**
```bash
python -m app.main
```
进入命令行交互界面，使用 `/start` `/stop` `/note` 等命令操作。

**方式二：Web UI（推荐）**
```bash
python -m app.web_main
```
打开浏览器访问 `http://127.0.0.1:5001`，三栏界面操作。

### 4.3 听课流程

1. 选择音源：电脑声音 / 麦克风 / 混合模式
2. 点击"开始听课"（Web）或输入 `/start`（CLI）
3. 系统开始录音，实时推送给 ASR
4. ASR 返回 final 句子 → 短句合并缓冲区
5. 合并条件满足 → 文本写入 RAG（JSONL 记录 + 向量索引更新）
6. Web UI 右侧面板通过 SSE 实时显示转写内容

### 4.4 提问流程

1. 学生输入问题
2. 问题文本向量化，在向量索引中检索 Top-5 相关片段
3. 过滤相似度 < 0.05 的噪声片段
4. 组装 RAG 上下文（课堂片段 + 问题）
5. 调用 LLM 生成回答
6. 输出回答 + 参考片段

### 4.5 混合音频模式

场景：腾讯会议 + 耳机——需要同时录到自己的麦克风声音和电脑里别人说话的声音。

原理：同时启动 `soundcard` 系统回环录音 + `sounddevice` 麦克风录音，实时用 numpy 混音后送入 ASR。

注意事项：
- 硬件限制：部分声卡在耳机模式下系统回环无法采集耳机输出，此时混合模式可能退化为只有麦克风
- 如果混合效果不好，建议改用外放扬声器 + 麦克风，或单独使用电脑声音模式

## 5. 达到的效果

### 5.1 功能效果

- ✅ 学生可以在课程进行中随时提问
- ✅ 回答基于当前已记录的课堂内容，不是凭空编造
- ✅ 老师讲课内容实时转写并进入检索索引
- ✅ 无音频环境时可通过手动输入演示完整流程
- ✅ 课程结束后一键清空，不同课程不串扰
- ✅ Web UI 三栏界面：左控制、中问答、右实时转写，操作直观
- ✅ 支持腾讯会议戴耳机场景（混合音频模式）

### 5.2 技术效果

- **延迟**：从 ASR 返回 final 句子到 Web UI 显示 < 1 秒（SSE 推送）
- **召回率**：短句合并 + Top-5 检索 + 低阈值，大多数课堂问题能找到相关片段
- **回答质量**：LLM 提示词针对 ASR 碎片化内容优化，从碎片中推断和综合

## 6. 特殊注意点

### 6.1 ASR 二进制协议

豆包大模型流式 ASR 使用 WebSocket 二进制协议。响应头中的 `flags` 字段决定是否包含序列号：
- `0x0`（NO_SEQUENCE）：无序列号
- `0x1`（POS_SEQUENCE）：包含 4 字节序列号
- `0x2`（NEG_SEQUENCE）：包含 4 字节序列号

必须根据 `flags` 判断是否读取序列号，不能靠 payload 大小推测，否则会解析失败报 JSON 错误。

### 6.2 Partial Text 去重

流式 ASR 会在最终确定前反复推送同一段 partial 文本。两层去重：
1. **ASR 客户端层**：`_last_partial` 字符串比对，相同内容直接跳过
2. **AudioListener 层**：1.5 秒节流 + 内容比对，防止同一段文本被重复写入 RAG

### 6.3 RAG 碎片化问题

ASR 输出的句子天然碎片化（一个语气词可能就是一条）。解决方案：
- 短句合并缓冲区：`_MERGE_MIN_CHARS=80`、`_MERGE_MAX_CHARS=500`、`_MERGE_FLUSH_SECONDS=5.0`
- 切片参数：`chunk_max_chars=800`、`chunk_overlap=120`
- 检索参数：`retrieve_top_k=5`、`min_similarity=0.05`
- LLM 提示词：明确要求"从碎片中推断和综合，不要因为碎片化就拒绝回答"

### 6.4 端口冲突

Web UI 默认使用端口 5001（避开 5000，macOS 上 5000 被 AirPlay 占用）。如需修改，编辑 `app/web_main.py`。

### 6.5 Windows 中文输入

PowerShell / CMD 中管道输入中文可能乱码，先运行：
```bash
chcp 65001
set PYTHONUTF8=1
```

### 6.6 系统声音捕获警告

运行时可能出现 `data discontinuity in recording` 警告，这是 Windows 音频子系统的短暂断续，不影响功能。如断续严重，可设置 `TA_AUDIO_BLOCK_SECONDS=1.5`。

## 7. 交互界面

### 7.1 Web UI（推荐）

启动后访问 `http://127.0.0.1:5001`，三栏暗色主题界面：

```
┌─────────────────────────────────────────────────────────────────┐
│  学生听课实时问答助手      ● 运行中 | 电脑播放声音 | 豆包流式ASR │
├──────────┬────────────────────────────┬─────────────────────────┤
│ 听课控制  │  提问与回答     [清空对话]  │  🔴 课堂实时记录        │
│          │                            │                         │
│ ▶ 开始听课│  问：力和加速度是什么关系？  │  牛顿第二定律说明...     │
│ ⏹ 停止听课│  答：根据课堂内容，加速度与   │  物体的加速度与合外力...  │
│ ────────  │  合外力成正比...            │  呃，大家注意这个...     │
│ 📝 手动记  │                            │                         │
│ 📋 最近记录│  [输入你的问题...     ] [发送]│  状态：正在识别...       │
│ ────────  │                            │                         │
│ 🗑 清空历史│                            │                         │
│ ────────  │                            │                         │
│ 音频来源  │                            │                         │
│ 🔊 电脑声音│                            │                         │
│ 🎤 麦克风  │                            │                         │
│ 🎧 混合模式│                            │                         │
└──────────┴────────────────────────────┴─────────────────────────┘
```

**特点：**
- 左栏：所有操作一键完成，音频来源切换即时生效
- 中栏：问答对话 + "清空对话"按钮，参考片段折叠显示
- 右栏：SSE 实时推送课堂转写，红色脉冲点标识录音中
- 暗色主题，适合长时间课堂使用

### 7.2 命令行（CLI）

```
学生听课实时问答助手
==================================
使用方式：后台听课，前台直接输入问题。
常用命令：/start 开始听课，/stop 停止，/note 手动记一段，/recent 最近记录，/clear 清空，/exit 退出。

你问 > /start
[听课状态] 已启动：电脑播放声音，豆包流式 ASR，WebSocket 真流式

你问 > /source mixed
已切换为：混合模式

你问 > 力和加速度是什么关系？
答：
根据课堂内容，物体的加速度与合外力成正比，与质量成反比。

参考课堂片段：
1. 牛顿第二定律说明，物体的加速度与合外力成正比，与质量成反比。（相关度 0.87）
```

## 8. API 接口一览（Web UI 用）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/start` | 开始听课 |
| POST | `/api/stop` | 停止听课 |
| POST | `/api/note` | 手动添加课堂记录 `{text: "..."}` |
| GET | `/api/recent?limit=20` | 获取最近课堂记录 |
| POST | `/api/clear` | 清空课堂历史 |
| POST | `/api/ask` | 学生提问 `{question: "..."}` |
| GET | `/api/status` | 获取当前状态 |
| POST | `/api/source` | 切换音源 `{source: "system/microphone/mixed"}` |
| GET | `/api/stream` | SSE 实时事件流 |

## 9. 验收标准

- [ ] 运行 `python -m app.web_main`，浏览器打开 `http://127.0.0.1:5001` 能看到三栏界面
- [ ] 点击"开始听课"后，右侧面板实时显示 ASR 转写内容
- [ ] 在中间面板提问，能获得基于课堂内容的回答
- [ ] 回答下方展示参考片段和相似度分数
- [ ] 切换到混合模式后，系统声音和麦克风声音都能被采集
- [ ] "清空历史"后再次提问不会检索到旧内容
- [ ] "清空对话"只清空问答区，不影响课堂记录
- [ ] 手动输入课堂内容后，能立即基于该内容提问
- [ ] 运行 `python -m app.main`，命令行交互流程正常
- [ ] 运行 `python -m unittest discover -s tests`，测试全部通过
